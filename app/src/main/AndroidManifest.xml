<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project3">
        <activity android:name=".SoundSelector"></activity>
        <activity android:name=".TimeSelector" />
        <activity android:name=".Notifications" />
        <activity android:name=".SettingsScreen" />
        <activity android:name=".DailyView" />
        <activity android:name=".YearSelector" />
        <activity android:name=".MonthSelector" />
        <activity android:name=".MonthlyView" />
        <activity android:name=".EditMettings" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>